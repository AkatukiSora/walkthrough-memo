pre-commit:
  parallel: true
  jobs:
    - name: gofmt
      glob: "*.go"
      run: gofmt -w {staged_files}

    - name: go vet
      run: go vet ./...

pre-push:
  parallel: true
  jobs:
    - name: go test
      run: go test ./...

    - name: golangci-lint
      run: golangci-lint run ./...
